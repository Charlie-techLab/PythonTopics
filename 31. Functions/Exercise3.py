"""
Declare a function called square, which takes any number as an argument, and
each time it is called, prints the square of that number (ie, the power 2 of
the value) on the screen.

The name of the argument that this function should take is a_number. Create
this variable and assign it any number.

You just have to define the function and create the variable, you don't have
to call the function afterwards.

============================================================================

Declara una función llamada cuadrado, que tome como argumento un número
cualquiera, y que cada vez que sea llamada, imprima en pantalla el cuadrado
de dicho número (es decir, la potencia 2 del valor).

El nombre del argumento que debe tomar dicha función es un_numero. Crea dicha
variable y asígnale un número cualquiera.

Solo debes definir la función y crear la variable, no debes invocar la función
luego.
"""

a_number = 12
def square(a_number):
    print(a_number**2)

un_numero = 12
def cuadrado(un_numero):
    print(un_numero**2)