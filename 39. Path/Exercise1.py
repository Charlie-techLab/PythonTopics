from pathlib import Path

"""
Store in the variable base_path, a Path object that points to the user's home
directory.

Remember to import Path from the pathlib module, and use the home() method.

===============================================================================

Almacena en la variable ruta_base, un objeto Path que señale el directorio base
del usuario.

Recuerda importar Path del módulo pathlib, y utilizar el método home().
"""

base_path = Path.home()
ruta_base = Path.home()