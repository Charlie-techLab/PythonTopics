from random import choice

"""
Use the random library's choice() method to get a random element from
the list of names below, and store the chosen name in a variable called
draw.

names = ["Carlos", "Julia", "Nicole", "Laura", "Mailen"]

============================================================================

Utiliza el método choice() de la librería random para obtener un elemento al
azar de la lista de nombres a continuación, y almacena el nombre escogido en
una variable llamada sorteo.

nombres = ["Carlos", "Julia", "Nicole", "Laura", "Mailen"]
"""

names = ["Carlos", "Julia", "Nicole", "Laura", "Mailen"]
draw = choice(names)

nombres = ["Carlos", "Julia", "Nicole", "Laura", "Mailen"]
sorteo = choice(nombres)